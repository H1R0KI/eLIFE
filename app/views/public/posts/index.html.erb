<p><%= flash[:notice] %></p>
<div class="h2">投稿一覧</div>
<table class="table table-hover">
  <tr>
    <th>タイトル</th>
    <th>説明</th>
  </tr>

  <% @posts.each do |post| %>
  <tr>
    <td><%= link_to post.title, post_path(post.id) %></td>
    <td><%= post.body %></td>
  </tr>
  <% end %>
</table>
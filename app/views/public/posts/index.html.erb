<p><%= flash[:notice] %></p>
<div class="h2">投稿一覧</div>
<table class="table table-hover">
  <tr>
    <th></th>
    <th>タイトル</th>
    <th>説明</th>
    <th>画像</th>
  </tr>

  <% @posts.each do |post| %>
  <tr>
    <td>
      <%= image_tag post.member.get_profile_image, size: "50x50" %></br>
      <%= post.member.name %>
    </td>
    <td><%= link_to post.title, post_path(post.id) %></td>
    <td><%= post.body %></td>
    <td><%= image_tag post.get_image, size: "50x50" %></td>
  </tr>
  <% end %>
</table>
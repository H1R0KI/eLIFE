<p><%= flash[:notice] %></p>

<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-3">
      <%= render 'tag_list', tag: @tag %>
    </div>

    <div class="col-9">
      <div class="h2 text-center">"<%= @tag.name %>" についての投稿 全<%= @posts.count %>件</div>
      <%= render 'post_list', post: @posts %>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-3">
      <%= image_tag @post.member.get_profile_image, size:"100x100"%></br>
      <%= link_to @post.member.name, member_path(@post.member_id) %>
    </div>
    <div class="col-6">
      <%= @post.created_at.strftime('%Y/%m/%d %H:%M') %></br>
      <%= @post.title %>
    </div>
    <div class="w-100">
    </div>
  </div>
  <div class="row">
    <div class="col">
      <%= @post.body %>
    </div>
  </div>
  <div class="row">
    <div class="col">
      <%= image_tag @post.get_image, size: "400x200" %>
    </div>
  </div>
</div>
新規投稿
<%= form_with model: Post.new do |f| %>
  <div class="form-group">
    <label for="post-title">タイトル</label>
    <p><%= f.text_field :title %></p>
  </div>
  <div class="form-group">
    <label for="post-body">説明</label>
    <p><%= f.text_field :body %></p>
  </div>
  <div class="form-group">
    <%= f.submit '投稿する', {class: "btn btn-success"} %>
  </div>
<% end %>
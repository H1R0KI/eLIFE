<% if current_member.following?(member) %>
  <%= link_to "フォロー外す", member_follows_path(member.id), method: :delete %>
<% else %>
  <%= link_to "フォローする", member_follows_path(member.id), method: :post %>
<% end %>